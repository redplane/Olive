<!DOCTYPE html>
<html>
<head>
    <title>Olives</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600|Roboto:300,400,500" rel="stylesheet">

    <style type="text/css">
        #page-container {
            font-family: "Roboto";
            display: flex;
        }

        #page-content {
            margin: 120px auto;
            text-align: center;
        }

        h3 {
            font-size: 18px;
            color: #333;
            font-weight: 300;
        }

        button {
            display: inline-block;
            font-size: $fs-small1;
            font-weight: 500;
            text-decoration: none;
            margin: 0;
            padding: 8px 21px;
            text-align: center;
            color: #fff;
            cursor: pointer;
            background-color: #ff;
            border: none;
            outline: none;
            border-radius: 2px;
            -webkit-transition: all 0.3s;
            -moz-transition: all 0.3s;
            transition: all 0.3s;
            overflow: hidden;
            background-color: #27ae60;
            border: 1px solid #27ae60;
            border-radius: 2px;
        }

            button:hover {
                background-color: #368055;
            }

        a {
            text-decoration: none;
            color: #fff;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div id="page-container">
        <div id="page-content">
            <h3>@ViewBag.Message</h3>
            
            @{
                if (ViewBag.IsError == null || !ViewBag.IsError)
                {
                    <button><a href="/signin">Sign in</a></button>
                }
            }
            
        </div>
    </div>

</body>
</html>